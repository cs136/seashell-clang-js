This directory contains patches to upstream LLVM/Clang that are required to build with
emscripten.  This directory also contains the appropriate clang/llvm-tblgen binaries
needed during the compilation process as the ones generated by the build process
are not runnable outside of node.
